export type User = {
  _id: string;
  name: string;
  accountNumber: string;
  balance: number;
  createdAt: string;
};

export type AdminUser = {
  _id: string;
  name: string;
  username: string;
};